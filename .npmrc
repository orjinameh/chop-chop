# Allows npm to resolve packages that haven't updated their peer dep declarations
# to explicitly support React 19 yet (e.g. next-auth, react-hot-toast).
# Safe to use — these packages work fine with React 19 at runtime.
legacy-peer-deps=true
